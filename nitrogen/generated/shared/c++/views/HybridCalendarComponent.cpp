///
/// HybridCalendarComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#include "HybridCalendarComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::calendar::views {

  extern const char HybridCalendarComponentName[] = "Calendar";

  HybridCalendarProps::HybridCalendarProps(const react::PropsParserContext& context,
                                           const HybridCalendarProps& sourceProps,
                                           const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    colorScheme([&]() -> CachedProp<ColorScheme> {
      try {
        const react::RawValue* rawValue = rawProps.at("colorScheme", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.colorScheme;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<ColorScheme>::fromRawValue(*runtime, value, sourceProps.colorScheme);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("Calendar.colorScheme: ") + exc.what());
      }
    }()),
    isBlue([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("isBlue", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.isBlue;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.isBlue);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("Calendar.isBlue: ") + exc.what());
      }
    }()),
    someCallback([&]() -> CachedProp<std::function<void()>> {
      try {
        const react::RawValue* rawValue = rawProps.at("someCallback", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.someCallback;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::function<void()>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.someCallback);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("Calendar.someCallback: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<margelo::nitro::calendar::HybridCalendarSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<margelo::nitro::calendar::HybridCalendarSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("Calendar.hybridRef: ") + exc.what());
      }
    }()) { }

  HybridCalendarProps::HybridCalendarProps(const HybridCalendarProps& other):
    react::ViewProps(),
    colorScheme(other.colorScheme),
    isBlue(other.isBlue),
    someCallback(other.someCallback),
    hybridRef(other.hybridRef) { }

  bool HybridCalendarProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("colorScheme"): return true;
      case hashString("isBlue"): return true;
      case hashString("someCallback"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridCalendarComponentDescriptor::HybridCalendarComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  react::Props::Shared HybridCalendarComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                     const react::Props::Shared& props,
                                                                     react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridCalendarShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridCalendarComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = dynamic_cast<HybridCalendarShadowNode&>(shadowNode);
    const HybridCalendarProps& props = concreteShadowNode.getConcreteProps();
    HybridCalendarState state;
    state.setProps(props);
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::calendar::views
